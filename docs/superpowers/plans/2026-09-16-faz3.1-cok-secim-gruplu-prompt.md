# Faz 3.1 — Çok-Seçim / Gruplu / Free-Text Prompt Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans. Steps use checkbox (`- [ ]`) syntax.

**Goal:** Faz 3.0 prompt kartlarını orca `AskUserQuestion` paritesine tamamla: multiSelect, free-text "Other", "Allow and don't ask again", Codex keystroke, ve (son) gruplu çok-soru.

**Architecture:** orca `buildAskAnswerKeys` birebir LumiKit'e port edilir (saf); telefon *selection* (`{indices, other}` per soru) yollar; host key group'ları **1000 ms aralıkla** (enjekte scheduler) PTY'ye yazar. Faz 3.0 hook-first + journal + relay altyapısı korunur.

**Tech Stack:** Swift 6 (strict concurrency), SwiftUI (iOS 17+), TypeScript (RelayServer — değişiklik YOK, prompt_respond zaten opak), XCTest + swift-testing.

## Global Constraints

- **main'e commit YOK.** Branch `feat/remote-orca-main`.
- **Kaynak: hook-first** (Faz 3.0). Relay `prompt`/`prompt_respond` zaten passthrough — **relay değişikliği YOK** (payload opak).
- **buildAskAnswerKeys (orca-kesin), LumiKit saf fonksiyon.** KeyGroup = `.raw(String)` | `.text(String)`. Sabitler: `ENTER="\r"`, `NEXT_TAB="\u{1b}[C"`, `PREV_ROW="\u{1b}[A"`, `NEXT_ROW="\u{1b}[B"`, `NOTES="\t"`. Algoritma spec §3 (birebir).
- **Paced writer:** grup i, `i*1000ms`'de yazılır; `.raw`→`writeInput(Data(s.utf8))`, `.text`→bracketed-paste (`\u{1b}[200~`+s+`\u{1b}[201~`). Scheduler enjekte (`KeystrokeScheduling`); yeni-cevap/`.exited`/`stop`/shutdown'da iptal.
- **prompt_respond genişletilmiş:** approval → `{optionId}` (Faz 3.0 korunur); question → `{selections:[{indices:[Int], other:String?}]}`.
- **ChatPrompt additive** (Faz 3.0 bozulmaz): `+multiSelect: Bool=false`, `+allowOther: Bool=false`, `+questions:[ChatPromptQuestion]=[]`. approval'a `dont-ask` seçeneği.
- **Kapsam:** multiSelect + free-text + don't-ask + Codex-port + gruplu. Yeni prompt TÜRÜ yok; alt-ajan yok (Faz 5). Codex uçtan-uca cihaz-doğrulaması YOK (port + birim test).
- **Model LumiKit'te tanımlanır, LumiMobileKit'e kopya** (Faz 1/2/3 kalıbı). LumiMobile literal kullanır.
- **Build/test:** `cd LumiPackages && swift test --scratch-path /tmp/lumi` (BARE suite adıyla --filter; target-prefix swift-testing'i eşleştirmez), `cd LumiMobile/LumiMobileKit && swift test`.
- **Launch-env:** app'i Claude bash'ından başlatma; cihaz + relay deploy kullanıcıya.
- **Build ortamı notu:** full rebuild yavaş → subagent 600s watchdog'unu aşabilir; Swift task'larını inline yazıp build'i background'da koşmak gerekebilir (review subagent'ları diff okur, stall etmez).

## File Structure

- **Create** `LumiPackages/Sources/LumiKit/NativeChat/AskAnswerKeys.swift` — saf `buildAskAnswerKeys` + Codex + `KeyGroup`/`AskSelection`/`AskPromptInput` tipleri.
- **Modify** `LumiPackages/Sources/LumiKit/Models/ChatPrompt.swift` — `ChatPromptQuestion` + additive alanlar + toDict.
- **Modify** `LumiPackages/Sources/LumiKit/NativeChat/PromptJournal.swift` — multiSelect/allowOther/questions doldurma.
- **Create** `LumiPackages/Sources/LumiKit/NativeChat/KeystrokeScheduling.swift` — scheduler protokolü + canlı impl.
- **Modify** `LumiPackages/Sources/LumiRemote/RemoteProtocol.swift` — prompt_respond selections decode.
- **Modify** `LumiPackages/Sources/LumiRemote/RemoteService.swift` — paced writer + selections→keygroups→PTY.
- **Modify** `LumiMobile/LumiMobileKit/Sources/LumiMobileKit/ChatPrompt.swift` — kopya + decode.
- **Modify** `LumiMobile/LumiMobileKit/Sources/LumiMobileKit/PhoneProtocol.swift` — promptRespondFrame selections.
- **Modify** `LumiMobile/LumiMobileKit/Sources/LumiMobileKit/AppModel.swift` — respondPrompt(selections).
- **Modify** `LumiMobile/App/MobileChatPromptCard.swift` — multiSelect/free-text/don't-ask; grouped (son).

---

### Task 1: `buildAskAnswerKeys` (LumiKit, saf) — Claude varyantı

**Files:**
- Create: `LumiPackages/Sources/LumiKit/NativeChat/AskAnswerKeys.swift`
- Test: `LumiPackages/Tests/LumiKitTests/AskAnswerKeysTests.swift`

**Interfaces:**
- Produces:
  - `enum KeyGroup: Equatable, Sendable { case raw(String); case text(String) }`
  - `struct AskSelection: Equatable, Sendable { var indices: [Int]; var other: String? }`
  - `struct AskQuestionInput: Equatable, Sendable { var question: String; var header: String?; var multiSelect: Bool; var optionLabels: [String] }`
  - `func buildAskAnswerKeys(questions: [AskQuestionInput], selections: [AskSelection]) -> [KeyGroup]`

- [ ] **Step 1: Write the failing test** (orca davranışı birebir)

`AskAnswerKeysTests.swift`:
```swift
import Testing
@testable import LumiKit

@Suite struct AskAnswerKeysTests {
    private func q(_ opts: [String], multi: Bool = false) -> AskQuestionInput {
        AskQuestionInput(question: "Q", header: nil, multiSelect: multi, optionLabels: opts)
    }

    @Test func singleQuestionSingleSelect() {
        let g = buildAskAnswerKeys(questions: [q(["A", "B"])], selections: [AskSelection(indices: [1], other: nil)])
        #expect(g == [.raw("2")])   // index 1 → "2", trailing Enter yok
    }

    @Test func singleQuestionMultiSelect() {
        let g = buildAskAnswerKeys(questions: [q(["A", "B", "C"], multi: true)],
                                   selections: [AskSelection(indices: [0, 2], other: nil)])
        #expect(g == [.raw("1"), .raw("3"), .raw("\u{1b}[C"), .raw("\r")])
    }

    @Test func groupedMultiQuestionSingleSelect() {
        let g = buildAskAnswerKeys(questions: [q(["A", "B"]), q(["C", "D", "E"])],
                                   selections: [AskSelection(indices: [1], other: nil), AskSelection(indices: [2], other: nil)])
        #expect(g == [.raw("2"), .raw("3"), .raw("\r")])
    }

    @Test func singleSelectFreeText() {
        let g = buildAskAnswerKeys(questions: [q(["A", "B"])], selections: [AskSelection(indices: [], other: "hello")])
        #expect(g == [.raw("3"), .text("hello"), .raw("\r")])   // typeSomething = optCount+1 = "3"
    }

    @Test func multiSelectWithOther() {
        let g = buildAskAnswerKeys(questions: [q(["A", "B"], multi: true)],
                                   selections: [AskSelection(indices: [0], other: "x")])
        #expect(g == [.raw("1"), .raw("3"), .text("x"), .raw("\r"), .raw("\u{1b}[C"), .raw("\r")])
    }

    @Test func unansweredMiddleQuestionSteps() {
        let g = buildAskAnswerKeys(questions: [q(["A"]), q(["C", "D"])],
                                   selections: [AskSelection(indices: [], other: nil), AskSelection(indices: [1], other: nil)])
        #expect(g == [.raw("\u{1b}[C"), .raw("2"), .raw("\r")])
    }
}
```

- [ ] **Step 2: Run FAIL** — `cd LumiPackages && swift test --scratch-path /tmp/lumi --filter AskAnswerKeysTests` → FAIL (yok).

- [ ] **Step 3: Implement** (orca `native-chat-ask.ts:204-249` birebir)

`AskAnswerKeys.swift`:
```swift
import Foundation

/// orca `buildAskAnswerKeys` port'u — AskUserQuestion cevabını PTY keystroke grup dizisine
/// çevirir. `.raw` ham tuş(lar), `.text` free-text (paste sanitize edilir). Saf/deterministik.
public enum KeyGroup: Equatable, Sendable { case raw(String); case text(String) }
public struct AskSelection: Equatable, Sendable {
    public var indices: [Int]; public var other: String?
    public init(indices: [Int], other: String?) { self.indices = indices; self.other = other }
}
public struct AskQuestionInput: Equatable, Sendable {
    public var question: String; public var header: String?; public var multiSelect: Bool; public var optionLabels: [String]
    public init(question: String, header: String?, multiSelect: Bool, optionLabels: [String]) {
        self.question = question; self.header = header; self.multiSelect = multiSelect; self.optionLabels = optionLabels
    }
}

private let ASK_ENTER = "\r"
private let ASK_NEXT_TAB = "\u{1b}[C"

/// orca answerLabels: seçili label'lar + trim'li other (option sırasında).
private func answerLabels(_ q: AskQuestionInput, _ sel: AskSelection?) -> [String] {
    let labels = (sel?.indices ?? []).compactMap { i -> String? in
        guard i >= 0, i < q.optionLabels.count else { return nil }
        let l = q.optionLabels[i]; return l.isEmpty ? nil : l
    }
    let other = (sel?.other ?? "").trimmingCharacters(in: .whitespacesAndNewlines)
    return other.isEmpty ? labels : labels + [other]
}

public func buildAskAnswerKeys(questions: [AskQuestionInput], selections: [AskSelection]) -> [KeyGroup] {
    let multiQuestion = questions.count > 1
    var groups: [KeyGroup] = []
    for (qi, q) in questions.enumerated() {
        let sel = qi < selections.count ? selections[qi] : nil
        let other = (sel?.other ?? "").trimmingCharacters(in: .whitespacesAndNewlines)
        let typeSomething = String(q.optionLabels.count + 1)
        if q.multiSelect {
            for i in (sel?.indices ?? []) { groups.append(.raw(String(i + 1))) }
            if !other.isEmpty { groups += [.raw(typeSomething), .text(other), .raw(ASK_ENTER)] }
            groups.append(.raw(ASK_NEXT_TAB))
        } else if !other.isEmpty {
            groups += [.raw(typeSomething), .text(answerLabels(q, sel).joined(separator: ", ")), .raw(ASK_ENTER)]
        } else if !(sel?.indices.isEmpty ?? true) {
            groups.append(.raw(String(sel!.indices[0] + 1)))
        } else if multiQuestion {
            groups.append(.raw(ASK_NEXT_TAB))
        }
    }
    let endsOnSubmitTab = multiQuestion || (questions.count == 1 && questions[0].multiSelect)
    if endsOnSubmitTab && !groups.isEmpty { groups.append(.raw(ASK_ENTER)) }
    return groups
}
```

- [ ] **Step 4: Run PASS** — `swift test --scratch-path /tmp/lumi --filter AskAnswerKeysTests` → 6/6.

- [ ] **Step 5: Commit**
```bash
git add LumiPackages/Sources/LumiKit/NativeChat/AskAnswerKeys.swift LumiPackages/Tests/LumiKitTests/AskAnswerKeysTests.swift
git commit -m "feat(remote): buildAskAnswerKeys port (Claude AskUserQuestion keystroke) — Faz 3.1"
```

---

### Task 2: `buildCodexAskAnswerKeys` (LumiKit, saf)

**Files:**
- Modify: `LumiPackages/Sources/LumiKit/NativeChat/AskAnswerKeys.swift`
- Test: `LumiPackages/Tests/LumiKitTests/AskAnswerKeysTests.swift` (aynı suite'e ekle)

**Interfaces:**
- Produces: `func buildCodexAskAnswerKeys(questions: [AskQuestionInput], selections: [AskSelection]) -> [KeyGroup]`.

- [ ] **Step 1: Write the failing test**
```swift
@Test func codexSingleSelectNoTrailingEnter() {
    let g = buildCodexAskAnswerKeys(questions: [q(["A", "B"])], selections: [AskSelection(indices: [1], other: nil)])
    #expect(g == [.raw("2")])   // digit hem seçer hem gönderir, Enter yok
}
@Test func codexGroupedSelect() {
    let g = buildCodexAskAnswerKeys(questions: [q(["A", "B"]), q(["C", "D"])],
                                    selections: [AskSelection(indices: [1], other: nil), AskSelection(indices: [0], other: nil)])
    #expect(g == [.raw("2"), .raw("1")])   // sonda Enter yok (hepsi cevaplı)
}
@Test func codexUnansweredBackspaceThenEnter() {
    let g = buildCodexAskAnswerKeys(questions: [q(["A", "B"])], selections: [AskSelection(indices: [], other: nil)])
    #expect(g == [.raw("\u{7f}"), .raw("\r"), .raw("\r")])  // son soru: \x7f + \r; hasUnanswered → + \r
}
@Test func codexNoteNavigatesAndTabs() {
    // 2 option, targetIndex=options.count=2, rowCount=3; nextSteps=2, previousSteps=1 → usePrevious, PREV_ROW×1
    let g = buildCodexAskAnswerKeys(questions: [q(["A", "B"])], selections: [AskSelection(indices: [], other: "note")])
    #expect(g == [.raw("\u{1b}[A"), .raw("\t"), .text("note"), .raw("\r")])
}
```

- [ ] **Step 2: Run FAIL** — `swift test --scratch-path /tmp/lumi --filter AskAnswerKeysTests` → yeni testler FAIL.

- [ ] **Step 3: Implement** (orca `:257-304` birebir)
```swift
private let ASK_PREV_ROW = "\u{1b}[A"
private let ASK_NEXT_ROW = "\u{1b}[B"
private let ASK_NOTES = "\t"

public func buildCodexAskAnswerKeys(questions: [AskQuestionInput], selections: [AskSelection]) -> [KeyGroup] {
    var groups: [KeyGroup] = []
    var hasUnanswered = false
    for (qi, q) in questions.enumerated() {
        let sel = qi < selections.count ? selections[qi] : nil
        let selectedIndex = sel?.indices.first
        let note = (sel?.other ?? "").trimmingCharacters(in: .whitespacesAndNewlines)
        if !note.isEmpty {
            let target = selectedIndex ?? q.optionLabels.count
            let rowCount = q.optionLabels.count + 1
            let nextSteps = target
            let previousSteps = rowCount - target
            let usePrevious = previousSteps < nextSteps
            let navKey = usePrevious ? ASK_PREV_ROW : ASK_NEXT_ROW
            let steps = usePrevious ? previousSteps : nextSteps
            for _ in 0..<steps { groups.append(.raw(navKey)) }
            groups += [.raw(ASK_NOTES), .text(note), .raw(ASK_ENTER)]
            continue
        }
        if let i = selectedIndex { groups.append(.raw(String(i + 1))); continue }
        hasUnanswered = true
        groups.append(.raw("\u{7f}"))
        if qi < questions.count - 1 { groups.append(.raw(ASK_NEXT_TAB)) } else { groups.append(.raw(ASK_ENTER)) }
    }
    if hasUnanswered { groups.append(.raw(ASK_ENTER)) }
    return groups
}
```

- [ ] **Step 4: Run PASS** — `swift test --scratch-path /tmp/lumi --filter AskAnswerKeysTests` → tümü.

- [ ] **Step 5: Commit**
```bash
git add LumiPackages/Sources/LumiKit/NativeChat/AskAnswerKeys.swift LumiPackages/Tests/LumiKitTests/AskAnswerKeysTests.swift
git commit -m "feat(remote): buildCodexAskAnswerKeys port (Faz 3.1)"
```

---

### Task 3: `ChatPrompt` genişletmesi (LumiKit + mobil kopya)

**Files:**
- Modify: `LumiPackages/Sources/LumiKit/Models/ChatPrompt.swift`
- Modify: `LumiMobile/LumiMobileKit/Sources/LumiMobileKit/ChatPrompt.swift`
- Test: `LumiPackages/Tests/LumiKitTests/ChatPromptTests.swift`, `LumiMobile/.../PromptDecodeTests.swift`

**Interfaces:**
- Produces: `ChatPromptQuestion{id,question,header?,multiSelect,allowOther,options}` (+toDict/decode); `ChatPrompt` yeni alanlar `multiSelect: Bool=false`, `allowOther: Bool=false`, `questions: [ChatPromptQuestion]=[]` (init additive, mevcut çağrı yerleri default'la derlenir).

- [ ] **Step 1: Failing test** (LumiKit toDict + mobil decode round-trip; multiSelect/allowOther/questions).
- [ ] **Step 2: Run FAIL.**
- [ ] **Step 3:** LumiKit `ChatPrompt`'e alanları ekle (init sonuna default'lu params), `toDict`'e `multiSelect`/`allowOther`/`questions`(map toDict) ekle; `ChatPromptQuestion` struct + toDict. Mobil kopyaya aynısı + `decode` (questions compactMap; multiSelect/allowOther `as? Bool ?? false`).
- [ ] **Step 4: Run PASS.**
- [ ] **Step 5: Commit** `feat(remote): ChatPrompt multiSelect/allowOther/questions (Faz 3.1)`.

> Not: mevcut Faz 3.0 ChatPrompt init çağrıları (PromptJournal, testler) default'larla derlenmeye devam etmeli — additive.

---

### Task 4: `PromptJournal` — multiSelect/allowOther/questions doldurma

**Files:**
- Modify: `LumiPackages/Sources/LumiKit/NativeChat/PromptJournal.swift`
- Test: `LumiPackages/Tests/LumiKitTests/PromptJournalTests.swift`

**Interfaces:** Consumes `ChatPrompt` (Task 3). `makeQuestion` genişler.

- [ ] **Step 1: Failing test** — tek soru multiSelect=true (tool_input `"multiSelect":true`) → `ChatPrompt.multiSelect==true`; iki soru → `questions.count==2`, her biri doğru; free-text (`freeText`/allowOther) alanı.
- [ ] **Step 2: Run FAIL.**
- [ ] **Step 3:** `makeQuestion`: `questions` array'ini işle. Tek soru → flat (`title`=q0.question, `options`, `multiSelect`=q0.multiSelect, `allowOther`= q0 free-text markörü), `questions=[]`. Çok soru → her soruyu `ChatPromptQuestion`'a map, `questions` doldur (flat `options` boş). allowOther markörü: orca `freeTextQuestionId` / tool_input'ta ilgili alan — §4.1 cihazda teyit; yoksa `false`.
- [ ] **Step 4: Run PASS.**
- [ ] **Step 5: Commit** `feat(remote): PromptJournal multiSelect/gruplu/free-text map (Faz 3.1)`.

---

### Task 5: `KeystrokeScheduling` + paced writer + prompt_respond selections (RemoteService)

**Files:**
- Create: `LumiPackages/Sources/LumiKit/NativeChat/KeystrokeScheduling.swift`
- Modify: `LumiPackages/Sources/LumiRemote/RemoteProtocol.swift`, `LumiPackages/Sources/LumiRemote/RemoteService.swift`
- Test: `LumiPackages/Tests/LumiRemoteTests/RemoteServicePromptTests.swift`

**Interfaces:**
- Produces: `protocol KeystrokeScheduling: Sendable { func sleep(_ duration: Duration) async throws }` + `struct LiveKeystrokeScheduler` (`Task.sleep`). `RemoteProtocol.decodePromptRespondSelections(_:) -> [AskSelection]?` (question dalı). RemoteService `keystrokeScheduler` init param (default live) + paced writer.

- [ ] **Step 1: Failing test** — fake scheduler (ani); multiSelect prompt (2 opt) → prompt_respond `{selections:[{indices:[0,1]}]}` → PTY'ye sırayla `"1","2","\u{1b}[C","\r"` yazılır (FakeTerminalServicing writtenInput birikimi). Enjekte scheduler ani çalıştığından sıra deterministik. Ayrıca approval Faz 3.0 yolu (optionId) bozulmamış.
- [ ] **Step 2: Run FAIL.**
- [ ] **Step 3:** `KeystrokeScheduling.swift` (protokol + live). RemoteService: `handlePromptRespond` — approval → Faz 3.0 tek keystroke; question → `decodePromptRespondSelections` → `buildAskAnswerKeys(questions, selections)` (provider'a göre Codex varyantı) → `writeKeyGroups(id, groups)` (her grup `keystrokeScheduler.sleep(.milliseconds(1000))` ile, `.raw`→writeInput, `.text`→bracketed-paste writeInput; Task, `promptWriteTasks[id]`'de tutulur; yeni cevap/`.exited`/stop/shutdown iptal). Sonra `journal.resolve` + emit. `FakeTerminalServicing.writtenInput` grupları biriktirir (mevcut Data birleştirme).
- [ ] **Step 4: Run PASS** + `swift test --scratch-path /tmp/lumi --filter LumiRemoteTests` (regresyon yok).
- [ ] **Step 5: Commit** `feat(remote): paced keystroke writer + prompt_respond selections (Faz 3.1)`.

> Not: `RemoteService(..., keystrokeScheduler:)` additive default (live). `buildAskAnswerKeys` question input'u ChatPrompt.questions'tan (veya tek-soru flat'tan) kurulur; provider = meta/prompt'tan (Claude default).

---

### Task 6: Telefon — PhoneProtocol promptRespondFrame(selections) + AppModel.respondPrompt(selections)

**Files:**
- Modify: `LumiMobile/LumiMobileKit/Sources/LumiMobileKit/PhoneProtocol.swift`, `AppModel.swift`
- Test: `LumiMobile/.../AppModelPromptTests.swift`

**Interfaces:**
- Produces: `PhoneProtocol.promptRespondFrame(sessionId:itemId:expectedRevision:selections:)` (question) — approval için Faz 3.0 optionId frame'i korunur (overload/ayrı fonksiyon). `AppModel.respondPromptSelections(_:itemId:revision:selections:)`.

- [ ] **Step 1: Failing test** — selections frame encode (`{selections:[{indices:[0,2],other:"x"}]}`), AppModel respond çağrısı doğru frame yollar (FakeRelayClient sent).
- [ ] **Step 2: Run FAIL.**
- [ ] **Step 3:** promptRespondFrame(selections) — payload `{sessionId,itemId,expectedRevision, selections:[{indices,other?}]}`. AppModel `respondPromptSelections`. Faz 3.0 approval `respondPrompt(optionId)` korunur.
- [ ] **Step 4: Run PASS** + full `swift test`.
- [ ] **Step 5: Commit** `feat(mobile): prompt_respond selections (multiSelect/free-text) — Faz 3.1`.

---

### Task 7: Telefon UI — tek-soru multiSelect + free-text + don't-ask

**Files:**
- Modify: `LumiMobile/App/MobileChatPromptCard.swift`
- (görsel; build + cihaz)

**Interfaces:** Consumes `model.prompts` (Task 3 alanları), `respondPromptSelections` (Task 6).

- [ ] **Step 1:** Kartı genişlet: `prompt.multiSelect` ise seçenekler toggle (checkbox) + "Submit (N)" butonu → `respondPromptSelections(selections: [{indices, other}])`. `prompt.allowOther` ise free-text `TextField` + gönder. approval'da `dont-ask` seçeneği (varsa) 3. buton. Tek-select (mevcut) tap'te anında `respondPromptSelections([{indices:[i]}])`.
- [ ] **Step 2:** `cd LumiMobile && xcodegen generate && xcodebuild ... build` → BUILD SUCCEEDED. `.xcodeproj` commit'e girmez.
- [ ] **Step 3: Commit** `feat(mobile): prompt kartı multiSelect/free-text/don't-ask (Faz 3.1)`.

> Cihaz: multiSelect toggle+Submit, free-text, don't-ask keystroke doğruluğu (orca haritası) teyit edilir.

---

### Task 8 (SON): Gruplu çok-soru UI (tab/stepping) — doğal durak/devir noktası

**Files:**
- Modify: `LumiMobile/App/MobileChatPromptCard.swift`
- (görsel; build + cihaz)

**Interfaces:** Consumes `prompt.questions` (Task 3). Host tarafı (Task 5 buildAskAnswerKeys) gruplu'yu zaten işliyor — bu task yalnız TELEFON UI'sı.

- [ ] **Step 1:** `prompt.questions.count > 1` ise: soru-soru stepping (orca `advanceGroupedQuestion` deseni) — her soru için seçim topla, sonda tek `respondPromptSelections(selections: [her soru için {indices,other}])`. Header'lı tab bar (orca `NativeChatQuestionCard` referans) opsiyonel; MVP: sıralı stepping (Soru 1/N → Sonraki → … → Gönder).
- [ ] **Step 2:** xcodegen + xcodebuild → BUILD SUCCEEDED.
- [ ] **Step 3: Commit** `feat(mobile): gruplu çok-soru prompt UI (Faz 3.1)`.

> Bu task en karmaşık/en düşük-frekans. Buraya kadar (Task 1-7) Faz 3.1'in çoğu değeri iner; Task 8 ayrı ele alınabilir/devredilebilir.

---

### Task 9: Full sweep + device handoff

- [ ] `cd LumiPackages && swift test --scratch-path /tmp/lumi` → yeşil.
- [ ] `cd LumiMobile/LumiMobileKit && swift test` → yeşil.
- [ ] `cd RelayServer && npm test` → yeşil (relay değişmedi; regresyon kontrolü).
- [ ] `cd LumiPackages && swift build -c release --product Lumi` → succeeds.
- [ ] Kullanıcı (launch-env): relay zaten Faz 3'te deploy'lu (prompt_respond payload opak, relay değişmedi) → Mac paketle + Finder'dan başlat + iOS cihaza kur. Cihaz: multiSelect, free-text, don't-ask, gruplu; keystroke doğruluğu + 1000ms pacing gerçek Claude'da teyit.

---

## Self-Review

**Spec coverage:** §3 buildAskAnswerKeys → Task 1; Codex → Task 2; §4 ChatPrompt genişletme → Task 3; PromptJournal → Task 4; paced writer + prompt_respond selections → Task 5; telefon wire → Task 6; multiSelect/free-text/don't-ask UI → Task 7; gruplu UI → Task 8; sweep → Task 9. Kapsam dışı (yeni tür/alt-ajan/Codex-cihaz) yok. Relay değişmez (prompt_respond opak) — doğru.

**Placeholder scan:** Task 1-2 tam kod (orca-kesin). Task 3-8 adımları kod-yönlü; Task 3/4/6'da "additive/mevcut kalıp" yönlendirmeleri codebase-özel, placeholder değil. `allowOther` markörünün tool_input kaynağı (Task 4) §4.1 cihaz-teyidine bağlı — spec'te işaretli belirsizlik, implementer `false` default'la iner.

**Type consistency:** `KeyGroup`/`AskSelection`/`AskQuestionInput` Task 1↔2↔5 aynı; `buildAskAnswerKeys(questions:selections:)` imzası Task 1↔5; `ChatPrompt` additive alanları Task 3↔4↔5↔6↔7↔8; `respondPromptSelections`/`promptRespondFrame(selections:)` Task 6↔7↔8; prompt_respond payload `{selections:[{indices,other?}]}` Task 5↔6. ✓

## Execution Handoff

**Plan complete and saved to `docs/superpowers/plans/2026-09-16-faz3.1-cok-secim-gruplu-prompt.md`.**
